use std::path::Path;

pub fn handle(
    cwd: &Path,
    title: Option<String>,
    body: Option<String>,
    found: Vec<String>,
    decided: Vec<String>,
    open: Vec<String>,
    file: Option<String>,
) -> anyhow::Result<()> {
    Ok(())
}
